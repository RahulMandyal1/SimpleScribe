import React from 'react';
import SignInForm from '../features/auth/SignInForm';

const LoginPage = () => {
  return (
    <section>
      <SignInForm />
    </section>
  );
};

export default LoginPage;
